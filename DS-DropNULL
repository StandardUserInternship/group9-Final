import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

df = pd.read_csv("train.csv")
df.head()
limitPer = len(df) * .80
df1 = df.dropna(thresh=limitPer, axis=1)


survived = df1['Survived']
survived.head()#Need to figure out how to allow user to select from Col_list
ages=df1['Age']
ages.head()
print(df1)
plt.scatter('Survived', 'Age', color='hotpink', alpha=0.5)
plt.show()
