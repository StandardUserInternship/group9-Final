import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

df = pd.read_csv('train.csv')
cols_list = ['PassengerId','Pclass', 'Age', 'SibSp', 'Parch','Fare']

pd.read_csv('train.csv', usecols = cols_list)

df = pd.read_csv('train.csv')
df1 = df.dropna()
x=print(input('SELECT FROM ' & cols_list )) #Need to figure out how to allow user to select from Col_list
y=print(input('SELECT FROM ' & #rows))
print(df1)
plt.scatter(x, y#,)
#, s=area, c=colors, alpha=0.5)
plt.show()
