import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

df = pd.read_csv("train.csv")
df.head()
df1 = df.dropna()
x=print(input('SELECT FROM ' & df.head() )) #Need to figure out how to allow user to select from Col_list
y=print(input('SELECT FROM ' & df.head()))
print(df1)
plt.scatter(x, y) 
#, s=area, c=colors, alpha=0.5)
plt.show()
